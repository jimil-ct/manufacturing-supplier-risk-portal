name: Demo Governance Gate

on:
  pull_request:
  push:

jobs:
  policy-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CognitivTrust demo governance check passed"
